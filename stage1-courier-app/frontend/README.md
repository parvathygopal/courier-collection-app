# Frontend — courier-collection-app

Run locally:

```bash
cd frontend
npm install
npm run dev
```

Environment:

- Create a `.env` file at `frontend/.env` with:

```env
VITE_API_URL=http://localhost:3001
```

Build:

```bash
npm run build
npm run preview
```
