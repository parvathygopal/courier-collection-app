# Frontend — courier-logistics-app

Run locally:

```bash
cd frontend
npm install
npm run dev
```

Environment:
- Create a `.env` file at `frontend/.env` with:

```
VITE_API_URL=http://localhost:3000
```

Build:

```bash
npm run build
npm run preview
```
